import React from 'react'

const Register = () => {
  return (
    <div>
      <p>This page is probably not needed since login component already has register feature.</p>
    </div>
  )
}

export default Register
